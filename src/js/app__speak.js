// const bot = document.querySelector(".app__container ");

// window.SpeechRecognition =
//   window.SpeechRecognition || window.webkitSpeechRecognition;

// const regocition = new SpeechRecognition();
// regocition.lang = "en-EN";

// regocition.addEventListener("end", regocition.start);

// regocition.start();

// regocition.addEventListener("result", (e) => {
//   const transScript = e.results;

//   if (transScript[0][0].transcript == "hi") {
//     bot.classList.add("show");
//     regocition.stop();
//   } else if (transScript[0][0].transcript == "close") {
//     bot.classList.remove("show");
//   }
// });
